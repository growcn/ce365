package com.loopj.android.image;import java.io.File;import com.growcn.ce365.util.AppConstant.Config;import android.os.Environment;public class SavePath {	// //这句话放在 WebImageCache.java中	// if (SavePath.checkSDCard()){	// diskCachePath = SavePath.getAppDir();	// }else{	// diskCachePath = appContext.getCacheDir().getAbsolutePath() +	// DISK_CACHE_PATH;	// }	// Android 检查sdCard是否存在	public static boolean checkSDCard() {		if (android.os.Environment.getExternalStorageState().equals(				android.os.Environment.MEDIA_MOUNTED))			return true;		else			return false;	}	public static String getAppDir() {		String sdpath = Environment.getExternalStorageDirectory() + "/"				+ Config.PRO_DIR + "/" + Config.IMAGE_DIR + "/";		File dir = new File(sdpath);		if (!dir.exists()) {			dir.mkdirs();		}		return sdpath;	}}